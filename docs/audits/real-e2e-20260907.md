# Real E2E corrections — 2026-09-07

Base: PR #30, master fca62efb8b7b07f41b6c65d3ed2c91e7fd0c5bcd.
NEW-18 and NEW-20 remain closed. No scheduler/security re-audit performed.

## Findings and changes

- ACT-01: Individual physical-code activation no longer checks legacy account chip capacity or prior account service expiration. Corporate capacity/service checks remain. Atomic token consumption, conditional chip/unit activation, profile ownership and medical completeness checks remain. A previously activated chip is also excluded by activatedAt, even if its status is inconsistent.
- INV-01: Stock counters now describe current unit status. Historical order references and dispatch/delivery dates do not count a delivered or activated unit as reserved or dispatched. Order references are preserved; no sold unit is released.
- OPS-01: History loads terminal orders initially, including operational orders whose linked dispatch is delivered. Stable ordering, pagination, code search and existing admin role guard remain. Initial list projects no customer identity fields.
- E2E-ACH-01: Post-purchase and My Orders share the existing signed-upload/raw-byte flow. JPG/PNG/WebP, maximum 5MB. PDF remains unsupported. No payment-proofs exception was added to the legacy upload route.
- UI-01: Explicit slate-950 text on the existing light slate/blue code card. No identifier changes.

## Read-only production evidence

The only unit linked to a delivered dispatch is delivered/not_activated, with its order reference retained. Its chip has no owner or activatedAt. Its claim token is active, unused and unexpired. No partial activation or data repair is indicated. The terminal-order history query finds one operational order linked to that delivered dispatch. No codes or personal information are included here.

## Verification

Local unit suite: 587 tests passed (82 files). Lint and typecheck passed.
Added PostgreSQL fixtures for separate-account gifts, zero capacity, same-code races, replay, invalid physical states, activatedAt takeover prevention, corporate capacity rollback, delivery, cancelled reservation release and historical order membership. Execution is required in PR CI before merge; local PostgreSQL is not installed.
Local production build: attempted with telemetry/release/source-map upload disabled and no inherited credentials. Blocked by Google Fonts network access (Inter / Instrument Serif). Production configuration was restored unchanged. Full CI build remains required.

## Manual continuation

After CI, merge, READY deployment and non-destructive smoke, the owner should retry the SAME physical code using their personal account, then test the physical QR/NFC. Do not create another real order, consume the code automatically or use this account for deletion tests.

No commercial GO. NEW-12 remains separate; full customer/admin/corporate/mobile E2E, authorized external delivery, isolated backup/restore, RPO/RTO and Storage strategy remain gates.
